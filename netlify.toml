[build]
  command = "npm install --legacy-peer-deps && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  CI = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
